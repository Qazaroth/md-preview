[git]
conventional_commits = false
tag_prefix = ""
ignore_merge_commits = true

[output]
file = "CHANGELOG.md"

[parser]
allow_unconventional = true

[changelog]
body = """
Release {{ version | trim_start_matches(pat="v") }} - {{ timestamp | date(format="%Y-%m-%d") }}

{% for group, commits in commits | group_by(attribute="group") %}
{% if commits | length > 0 %}
### {{ group }}

{% for commit in commits %}
- {{ commit.message }}
{% endfor %}

{% endif %}
{% endfor %}

{% set has_ungrouped = false %}
{% for commit in commits %}
{% if commit.group == "" %}
{% if not has_ungrouped %}
### Other Changes
{% set has_ungrouped = true %}
{% endif %}
- {{ commit.message }}
{% endif %}
{% endfor %}
"""

[sections]
feat = { title = "Features", kind = "feat" }
fix  = { title = "Bug Fixes", kind = "fix" }
chore = { title = "Chores", kind = "chore" }
docs  = { title = "Documentation", kind = "docs" }
